q_ora_abastecimiento_tables = """SELECT OWNER,TABLE_NAME,TABLE_LOCK,SAMPLE_SIZE,LAST_ANALYZED FROM ALL_TABLES WHERE TABLE_NAME LIKE '%SEACE%' """
q_ora_abastecimiento_columns = """SELECT OWNER,TABLE_NAME,COLUMN_NAME,DATA_TYPE,DATA_LENGTH,DATA_PRECISION,COLUMN_ID,NUM_DISTINCT,LOW_VALUE,HIGH_VALUE,DENSITY,NUM_NULLS,LAST_ANALYZED,SAMPLE_SIZE FROM ALL_TAB_COLUMNS WHERE TABLE_NAME LIKE '%SEACE%' """ 
q_ora_salud_tables = """SELECT OWNER,TABLE_NAME,TABLE_LOCK,SAMPLE_SIZE,LAST_ANALYZED FROM ALL_TABLES WHERE TABLE_NAME LIKE '%COVID%' """
q_ora_salud_columns = """SELECT OWNER,TABLE_NAME,COLUMN_NAME,DATA_TYPE,DATA_LENGTH,DATA_PRECISION,COLUMN_ID,NUM_DISTINCT,LOW_VALUE,HIGH_VALUE,DENSITY,NUM_NULLS,LAST_ANALYZED,SAMPLE_SIZE FROM ALL_TAB_COLUMNS WHERE TABLE_NAME LIKE '%COVID%' """ 
q_ora_domains = """SELECT DOMINIO AS DOMINIO,count(*) AS CANTIDAD_TABLAS,sum(SIZE_GIGA) AS SIZE_DOMINIO,sum(CANT_REG) AS NUMBER_RECORDS,MAX(PERIODICIDAD) AS PERIODICIDAD FROM SCH_UNIVERSAL.LISTADO_OBJECT WHERE DOMINIO IS NOT NULL GROUP BY DOMINIO ORDER BY DOMINIO"""
q_ora_tablesbydomain = """SELECT * FROM SCH_UNIVERSAL.LISTADO_OBJECT"""
